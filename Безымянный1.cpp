#include <iostream>
#include <math.h>
#include <vector>
#include <iterator>
#include <ctime>
#include <cstdlib>

using namespace std;

int main() {
	srand (time(0));
	int i, a, b, diff, N;
	float avg; 
	vector<int> arr;
	cout << "Enter the size range: ";
	cin >> N;
	
	system("pause");
	return 0;
}
